import React from 'react';
// native-base
import {VStack, HStack, ScrollView} from 'native-base';

function PianoGoGo() {
  return <VStack flex={1} bgColor="white"></VStack>;
}

export default PianoGoGo;
